
REACT_APP_API_URL=https://hotstinder.vercel.app

# 서버 환경변수
NODE_ENV=production
BNET_CLIENT_ID=your_bnet_client_id_here
BNET_CLIENT_SECRET=your_bnet_client_secret_here
BNET_CALLBACK_URL=https://hotstinder.vercel.app/api/auth/bnet/callback
BNET_REGION=kr
JWT_SECRET=your_jwt_secret_here
FRONTEND_URL=https://hotstinder.vercel.app
MONGODB_URI=your_mongodb_connection_string_here

# 사용 방법:
# 1. Battle.net Developer Portal에서 CLIENT_ID와 CLIENT_SECRET 복사
# 2. MongoDB Atlas에서 연결 문자열 복사
# 3. JWT_SECRET은 랜덤한 긴 문자열로 설정 (예: openssl rand -base64 32)
# 4. Vercel Dashboard에서 각 환경변수를 개별적으로 추가

# 주의사항:
# - 각 환경변수는 Vercel Dashboard에서 개별적으로 추가해야 합니다
# - 이 파일을 그대로 업로드하는 것이 아니라 내용을 복사해서 사용하세요
# - 실제 값들로 your_xxx_here 부분을 교체하세요 